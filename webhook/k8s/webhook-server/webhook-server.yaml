apiVersion: v1
kind: Service
metadata:
    name: "webhook-svc"
spec:
    type: ClusterIP
    selector:
        app: "webhook-server"
    ports:
    -   port: 8080
        targetPort: 8080

---

apiVersion: apps/v1
kind: Deployment
metadata:
    name: "webhook-server"    
spec:
    replicas: 1
    selector:
        matchLabels:
            app: "webhook-server"
    template:
        metadata:
            labels:
                app: "webhook-server"
        spec:
            containers:
            -   image: "webhook-server"
                name: "webhook-server"