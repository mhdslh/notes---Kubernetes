apiVersion: v1
kind: Pod
metadata:
    name: test1
spec:
    containers:
    - image: ubuntu:latest
      name: ubuntu
      command: ["/bin/bash", "-c"]
      args: ["tail -f /dev/null"]

---

apiVersion: v1
kind: Pod
metadata:
    name: test2
spec:
    containers:
    - image: ubuntu
      name: ubuntu
      command: ["/bin/bash", "-c"]
      args: ["tail -f /dev/null"]
